{
  "private": true,
  "scripts": {
    "build": "node -r esm configbuild.js && preact build",
    "serve": "preact serve",
    "dev": "node -r esm configbuild.js && preact -p 80 watch",
    "dockerDev": "npm i && node -r esm configbuild.js && preact -p 3000 watch"
  },
  "devDependencies": {
    "esm": "^3.0.32",
    "object-merge": "^2.5.1",
    "preact-cli": "^2.0.0"
  },
  "dependencies": {
    "preact": "^8.2.1",
    "preact-material-components": "^1.4.0",
    "preact-router": "^2.5.5",
    "unistore": "^3.0.5"
  }
}
