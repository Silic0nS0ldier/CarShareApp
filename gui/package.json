{
  "private": true,
  "scripts": {
    "build": "preact build",
    "serve": "preact serve",
    "dev": "npm i && preact -p 3000 watch"
  },
  "devDependencies": {
    "preact-cli": "^2.0.0"
  },
  "dependencies": {
    "preact": "^8.2.1",
    "preact-router": "^2.5.5",
    "preact-material-components": "^1.4.0"
  }
}
